---
apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: aifas-frontend-build
  annotations:
    description: |
      Build (Docker strategy) and ImageStream for the AIFAS frontend (Vue/Vite SPA).
      Builds from the `frontend/` context directory and pushes to the
      `aifas-frontend:latest` ImageStream tag in the tools namespace. Environment
      tags (dev/test/prod) are produced by promoting `latest` with `oc tag`
      (see openshift/README.md).
    tags: aifas,frontend,nodejs,vue
labels:
  app: aifas
  component: frontend
parameters:
  - name: REPO_OWNER
    description: GitHub organization / owner
    value: bcgov
    required: true
  - name: REPO_NAME
    description: GitHub repository name
    value: ai-form-assistant-showcase
    required: true
  - name: SOURCE_GIT_URL
    description: Git URL to build from
    value: https://github.com/bcgov/ai-form-assistant-showcase.git
    required: true
  - name: SOURCE_GIT_REF
    description: Git branch, tag, or commit to build
    value: main
    required: true
  - name: OUTPUT_TAG
    description: ImageStream tag the build pushes to
    value: latest
    required: true
objects:
  # Image registry for the built frontend image
  - apiVersion: image.openshift.io/v1
    kind: ImageStream
    metadata:
      name: aifas-frontend
      labels:
        app: aifas
        component: frontend
    spec:
      # Allow local (in-cluster) name resolution so Deployments can reference it directly
      lookupPolicy:
        local: true

  # Docker-strategy build that compiles the SPA and bakes it into an nginx image
  - apiVersion: build.openshift.io/v1
    kind: BuildConfig
    metadata:
      name: aifas-frontend
      labels:
        app: aifas
        component: frontend
    spec:
      completionDeadlineSeconds: 1800
      failedBuildsHistoryLimit: 3
      successfulBuildsHistoryLimit: 3
      output:
        to:
          kind: ImageStreamTag
          name: aifas-frontend:${OUTPUT_TAG}
      resources:
        requests:
          cpu: 500m
          memory: 1Gi
        limits:
          cpu: "1"
          memory: 2Gi
      source:
        type: Git
        git:
          uri: ${SOURCE_GIT_URL}
          ref: ${SOURCE_GIT_REF}
        contextDir: frontend
      strategy:
        type: Docker
        dockerStrategy:
          dockerfilePath: Dockerfile
      triggers:
        - type: ConfigChange
